rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Require authentication for all data access by default
      allow read, write: if request.auth != null;
    }
  }
} 